# Configurando MirrorList
reflector --verbose --latest 5 --sort rate --save /etc/pacman.d/mirrorlist

# Verificando Python
pacman -Sy python3 --noconfirm

# Instalando SpeedTest
pacman -Sy speedtest-cli --noconfirm

# Config Teclado
loadkeys br-abnt2

# Muda o idioma do ambiente live
echo "pt_BR.UTF-8 UTF-8" > /etc/locale.gen

# Acrescentando o idioma e codificação PTBR
export LANG=pt_BR.UTF-8

# Verifica o tipo de inicialização
ls /sys/firmware/efi/efivars
    --> Retorna
            BIOS
            UEFI

# Teste de rede
ping archlinux.org
    --> Verifica a rede
            Boa
            Ruim
            No work

# Set Relogio do Sistema
timedatectl set-ntp true
